# ============================================================
#  Monolith .gitignore — 什么该推送，什么不该推送
# ============================================================

# ────────────────────── 依赖目录（不推送）──────────────────────
node_modules/

# ────────────────────── 构建产出（不推送）──────────────────────
dist/
.wrangler/

# ────────────────────── 环境变量与密钥（绝不推送）──────────────────
# 所有 .env 文件都可能含密钥，禁止推送
.env
.env.*
!.env.example

# Wrangler 本地密钥（含 ADMIN_PASSWORD / JWT_SECRET 等）
.dev.vars

# ────────────────────── 系统与编辑器临时文件（不推送）──────────────
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE 配置
.idea/
.vscode/
*.code-workspace

# ────────────────────── AI 工具私有数据（绝不推送）──────────────────
# 通用记忆库（Aider / Cline / 其他）
.agents/

# Trae 项目私有数据：memory/ 含密码教训、PAT 用法、踩坑记录等敏感信息
# rules/ skills/ 也仅供本机女仆使用，不入仓库
.trae/

# 各家 AI 编辑器的项目入口与全局配置（含个人偏好/上下文，不入仓库）
CLAUDE.md
GEMINI.md
AGENTS.md
opencode.json

# 临时调试目录
.playwright-mcp/
.wiki-tmp/

# ────────────────────── 日志与调试产物（不推送）──────────────────
*.log
npm-debug.log*

# ────────────────────── 测试与临时输出（不推送）──────────────────
tmp/
tests/output/

# ────────────────────── 必须推送的重要文件（白名单提醒）─────────────
# 以下文件虽然容易被误加 gitignore，但必须跟踪：
# ✅ package-lock.json    — 锁定依赖版本，CI 的 npm ci 依赖它
# ✅ client/functions/    — Pages Functions 反向代理，漏掉则 /api/* 回退为首页 HTML
# ✅ scripts/             — 部署脚本 (deploy-cloudflare.mjs)
# ✅ .github/             — CI/CD workflows、Issue/PR 模板、分支保护
# ✅ .coderabbit.yaml     — CodeRabbit 代码审查配置
# ✅ SECURITY.md          — 安全政策
# ✅ PRIVACY.md           — 隐私政策
# ✅ README.md / LICENSE  — 项目门面
